import { Routes } from '@angular/router';
import { MainComponent } from './main.component';

export const mainDashboradRoutes: Routes = [
    {
        path: '',
        component: MainComponent
    }
];
